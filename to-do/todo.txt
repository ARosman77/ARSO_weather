Implement something like this to decode weather:

teststr="overcast_lightRA"
#teststr="overcast"

description=teststr.split("_")
clouds=description[0] if 0 < len(description) else None

s=description[1] if 1 < len(description) else None

if s != None:
  drops="".join([c for c in s if c.isupper()])
else:
	drops=""

print(clouds)
print(drops)